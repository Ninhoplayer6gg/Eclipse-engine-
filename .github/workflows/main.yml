name: Build Eclipse Engine Libs

on:
  workflow_dispatch: 

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout mkxp-z
        uses: actions/checkout@v4
        with:
          repository: mkxp-z/mkxp-z
          submodules: recursive

      - name: Install Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y meson ninja-build pkg-config

      - name: Patch Meson for Android (O Retorno do Fix Gráfico)
        run: |
          # Avisa o motor para usar o GLESv2 (Android) e não o GL (Desktop)
          sed -i "s/dependency('gl')/meson.get_compiler('c').find_library('GLESv2')/g" meson.build
          sed -i "s/dependency('GL')/meson.get_compiler('c').find_library('GLESv2')/g" meson.build

      - name: Create Cross-File
        run: |
          mkdir -p cross
          cat <<EOF > cross/android-armv7.txt
          [binaries]
          c = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/armv7a-linux-androideabi24-clang'
          cpp = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/armv7a-linux-androideabi24-clang++'
          ar = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-ar'
          strip = '$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-strip'
          pkgconfig = '/usr/bin/false'
          pkg-config = '/usr/bin/false'

          [host_machine]
          system = 'android'
          cpu_family = 'arm'
          cpu = 'armv7a'
          endian = 'little'
          EOF

      - name: Compile for armeabi-v7a
        run: |
          # O forcefallback baixa o PhysFS e o release otimiza a bateria
          meson setup builddir \
            --cross-file cross/android-armv7.txt \
            --wrap-mode=forcefallback \
            -Dbuildtype=release
          ninja -C builddir

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: eclipse-engine-libs-armv7
          path: builddir/*.so
          
